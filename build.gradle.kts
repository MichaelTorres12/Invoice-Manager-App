plugins {
    id("com.android.application") apply false
    id("org.jetbrains.kotlin.android") apply false
    // No need to include com.google.gms.google-services here
}

// Remove any 'allprojects' or 'subprojects' blocks
